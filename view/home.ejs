<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <link href="/output.css" rel="stylesheet">
</head>
<body class="relative min-h-screen bg-gradient-to-br from-purple-100 via-indigo-50 to-white flex items-center justify-center px-6 py-12 overflow-hidden">

  
  <div class="absolute top-[-10%] left-[-10%] w-72 h-72 bg-purple-300 rounded-full opacity-40 filter blur-3xl animate-pulse"></div>
  <div class="absolute bottom-[-10%] right-[-10%] w-96 h-96 bg-pink-300 rounded-full opacity-30 filter blur-3xl animate-pulse"></div>


  <main class="relative w-full max-w-lg bg-white/70 backdrop-blur-3xl rounded-3xl shadow-2xl border border-gray-200 p-10 transition-all duration-500 hover:shadow-3xl font-serif">

   
    <h1 class="text-5xl font-extrabold text-center mb-4 bg-gradient-to-r from-[#1E3A8A] to-[#c89c5d] bg-clip-text text-transparent ">
      URL Shortener
    </h1>

    
    <p class="text-center text-gray-700 mb-8 max-w-md mx-auto font-serif">
      Convert long links into sleek, shareable URLs instantly. Clean, modern, and student-friendly.
    </p>

    
    <% if (locals.id) { %>
      <div class="bg-white/90 border border-green-200 text-green-800 px-5 py-3 rounded-2xl mb-6 text-center shadow-md hover:shadow-lg transition transform duration-300">
        <a href="<%=baseurl%><%=port%>/url/<%=id %>" target="_blank"
           class="underline font-semibold text-indigo-600 hover:text-indigo-800 transition">
           <%=baseurl%><%=port%>/url/<%=id %>
        </a>
      </div>
    <% } %>

    <!-- Form -->
    <form method="post" action="/url" class="space-y-5">
      <div>
        <label for="url" class="block text-sm font-medium text-gray-700 mb-2">
          Enter Original URL
        </label>
        <input type="text" id="url" name="url" placeholder="https://example.com" required
          class="w-full px-5 py-3 rounded-2xl border border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-2 focus:ring-indigo-300 outline-none transition duration-200"/>
      </div>
      <button type="submit"
        class="w-full py-3 bg-gradient-to-r from-[#1E3A8A] via-[#c89c5d] to-[#E598C0] text-white font-semibold rounded-2xl shadow-lg hover:scale-105 transform transition-all duration-200">
        Generate 
      </button>
    </form>

   
    

  </main>
</body>
</html>
